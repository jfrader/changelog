# Dependencies
node_modules/

# Build output is COMMITTED so the package works when installed via git
# (yarn 1 does not run lifecycle scripts for git dependencies). Rebuild with
# `npm run build` before committing when src/ changes.
# dist/

*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
node_modules/

